apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-react.fullname" . }}
  labels:
    {{- include "admin-react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "admin-react.selectorLabels" . | nindent 4 }}
