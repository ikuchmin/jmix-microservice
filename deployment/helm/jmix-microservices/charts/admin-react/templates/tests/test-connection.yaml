apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admin-react.fullname" . }}-test-connection"
  labels:
    {{- include "admin-react.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admin-react.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
