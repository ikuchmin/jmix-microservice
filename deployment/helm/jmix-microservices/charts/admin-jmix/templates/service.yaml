apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-jmix.fullname" . }}
  labels:
    {{- include "admin-jmix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "admin-jmix.selectorLabels" . | nindent 4 }}
