apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jmix-microservices.fullname" . }}-test-connection"
  labels:
    {{- include "jmix-microservices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jmix-microservices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
