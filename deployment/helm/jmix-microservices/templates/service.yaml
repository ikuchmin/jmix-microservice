apiVersion: v1
kind: Service
metadata:
  name: {{ include "jmix-microservices.fullname" . }}
  labels:
    {{- include "jmix-microservices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jmix-microservices.selectorLabels" . | nindent 4 }}
